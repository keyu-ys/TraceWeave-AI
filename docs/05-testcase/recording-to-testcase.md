# 从录制到用例

## 核心观点

录制得到的是一串请求交换，不是可维护测试用例。转换过程至少包含四步：

1. 过滤噪声
2. 切分业务步骤
3. 参数化依赖链
4. 补断言与执行元信息

## 过滤噪声

去掉心跳、埋点、静态资源、不可复现副作用流量。

## 切分步骤

按业务意图和依赖顺序而不是纯时间顺序切分。

## 参数化

把硬编码 ID、查询条件、分页游标、资源引用转成变量流。

## 补断言

至少补：

- 基本成功断言
- 关键字段存在断言
- 关键链路变量提取断言
