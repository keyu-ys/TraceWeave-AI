# 断言策略

## 默认断言

任何回放步骤都至少应有：

- 请求成功断言
- 响应结构存在性断言

## 增强断言

在具备足够上下文时追加：

- 关键字段值断言
- 列表长度断言
- 状态流转断言
- 依赖链一致性断言

## 警告型断言

不是所有异常都应让用例直接失败。对边界波动、兼容字段或提示信息可使用 warning 级断言。

## AI 参与规则

- AI 可以建议断言
- AI 不应删除已有高置信断言
- AI 新增断言必须说明依据
