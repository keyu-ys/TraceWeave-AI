# 回放准备度

## 目标

不是所有抓包请求都适合回放，需要先给出准备度判断。

## 适合回放的信号

- 请求具有稳定业务目标
- 关键参数可被提取或替换
- 响应结构可被断言
- 路径和方法不是一次性随机资源

## 不适合回放的信号

- 心跳、埋点、上报类请求
- 强依赖本地会话状态却无法复现
- 文件流、二进制上传且缺少稳定输入
- 完全依赖不可控外部副作用

## 准备度等级

- `ready`
- `needs-doc`
- `needs-parameterization`
- `analysis-only`
