# 异常检测

## 目标

在请求级、步骤级、用例级三个层面发现异常。

## 请求级异常

- 响应超时
- 非预期状态
- 关键字段缺失
- 结构与文档不一致
- 文本错误提示

## 步骤级异常

- 提取器为空
- 注入器未命中
- 断言冲突
- 上下文缺项

## 用例级异常

- 依赖链断裂
- 多步失败共因
- 运行时上下文不完整
- 同类失败重复出现

## 输出

异常检测输出不应直接等同缺陷。它先输出异常事实，再交给聚合逻辑决定是否形成 `DefectRecord`。
